import React from 'react';
import {Text, View} from 'react-native';

const AboutUs = (props) => {

    return (
        <View>
            <Text>About us2</Text>
        </View>
    );
};

export default AboutUs;
